'use client';
import React from 'react';

const GetStartedButton = () => {
    return (
        <>
            {/* Adding some space below the button using margin */}
            <div style={{ marginTop: '30px' }}>
                <button className="btn btn-primary">Get Started</button>
            </div>
        </>
    );
};

export default GetStartedButton;
